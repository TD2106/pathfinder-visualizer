import { GridPosition } from '../type/NodeData';

export const MIN_GRID_DIMENSION_SIZE = 10;
export const MAX_GRID_DIMENSION_SIZE = 50;
export const EMPTY_GRID_POSITION: GridPosition = {
    rowIndex: -1,
    colIndex: -1,
};
